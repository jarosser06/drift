---
description: Run the project test suite using pytest with optional coverage
---

1. Run the test suite:
   ```bash
   ./test.sh --embedded $ARGUMENTS
   ```

   Arguments:
   - `--coverage` - Run with coverage report (requires 90% minimum coverage)
