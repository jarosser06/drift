---
description: Run all linters (formatting, linting, type checking)
---

1. Run the linting suite:
   ```bash
   ./lint.sh $ARGUMENTS
   ```

   Arguments:
   - `--fix` - Auto-fix formatting issues where possible (black, isort)
